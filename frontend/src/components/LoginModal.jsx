import LoginForm from "./LoginForm";
import Modal from "../styles/Modal.styled";

const LoginModal = () => {
  return (
    <Modal>
      <LoginForm />
    </Modal>
  );
};
export default LoginModal;
